import "@wailsio/runtime";

// Required: initializes Wails IPC and runtime (including --wails-draggable support).
// Without this, drag regions and runtime features won't work in Nuxt SPA mode.
export default defineNuxtPlugin(() => {});
